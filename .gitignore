# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Dữ liệu runtime (sinh khi chạy)
data/chat/
data/uploads/
data/datasets/_nhanes_cache/

# Dữ liệu MIMIC-IV — DUA PhysioNet cấm tái phân phối, KHÔNG BAO GIỜ commit
# (NHANES công khai thì ngược lại: file merged vẫn giữ trong repo làm bằng chứng)
data/mimic/
data/mimiciv/
**/mimic*/
mimic*.zip
mimic*.csv*
mimic*.parquet
*_mimic*
bypass_*.txt

report/
api.log
*.log

# Backup tự động của cơ sở tri thức
*.json.bak

# Prediction arrays (aggregate scores cho ROC) — tái sinh được bằng script
roc_predictions*.json.gz
**/roc_predictions.json.gz

# Hệ điều hành / IDE
.DS_Store
.idea/
.vscode/
