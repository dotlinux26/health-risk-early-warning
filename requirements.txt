pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
lightgbm>=4.0
shap>=0.43
joblib>=1.3
statsmodels>=0.14        # STL decomposition (tùy chọn)
torch>=2.0               # LSTM đối chứng (tùy chọn)
chronos-forecasting>=1.2 # time-series foundation model cho Tầng 1 (tùy chọn)
optuna>=3.5              # tối ưu siêu tham số (tùy chọn)
pyreadstat>=1.2          # đọc NHANES .XPT (tùy chọn)
fastapi>=0.104           # API
uvicorn[standard]>=0.24
python-multipart>=0.0.9  # upload file qua API
PyMuPDF>=1.24            # trích xuất PDF
python-docx>=1.1         # trích xuất DOCX
requests>=2.31           # LLM extractor (tùy chọn)
streamlit>=1.30          # dashboard demo (tùy chọn)
